{
  "configurations": [
    {
      "type": "node",
      "name": "vscode-jest-tests",
      "request": "launch",
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen",
      "runtimeExecutable": "yarn",
      "cwd": "${workspaceFolder}",
      "args": ["test", "test", "--watchAll=false", "--coverage=true", "--runInBand"]
    }
  ]
}
